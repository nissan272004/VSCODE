#include <iostream>
#include <string>

using namespace std;

int main() {
    string query;
    while (getline(cin, query)) {
        cout << "NO" << endl;
        cout.flush();
    }
    return 0;
}
